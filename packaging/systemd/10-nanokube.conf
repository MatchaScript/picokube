# kubelet.service ships without an [Install] section, so it cannot be enabled.
# Upholds= lets multi-user.target start crio and kubelet without an enablement
# symlink.
[Unit]
Upholds=crio.service kubelet.service
